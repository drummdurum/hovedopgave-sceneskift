<!DOCTYPE html>
<html lang="da">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - SceneSkift</title>
  <link rel="icon" href="/compentens/image/logo rundt.png">
  <link rel="stylesheet" href="/css/output.css">
</head>
<body class="flex flex-col min-h-screen" style="background-color: var(--color-light); font-family: var(--font-body);">
  
  <!-- Header fixed på toppen med transparent baggrund -->
  <header class="sticky top-0 z-50" style="background-color: transparent; z-index: 50;">
    <%- include('../header/header') %>
  </header>
    
  <!-- Hero sektion med baggrundsbillede -->
  <section class="relative min-h-[50vh] md:min-h-[40vh] flex flex-col" style="background-image: url('/compentens/image/forside.webp'); background-size: cover; background-position: center; background-repeat: no-repeat; margin-top: -100px; margin-bottom: 20px;">
    
    <!-- Dark overlay -->
    <div class="absolute inset-0" style="background-color: rgba(0, 0, 0, 0.5); z-index: 0;"></div>
    
    <!-- Hero indhold -->
    <div class="flex flex-col items-center justify-center w-full px-4 md:px-6 flex-grow relative" style="z-index: 1;">
      <div class="text-center" style="padding: 60px 16px;">
        <h1 class="text-2xl sm:text-3xl md:text-5xl lg:text-6xl font-bold mb-2 md:mb-4" style="font-family: var(--font-heading); color: var(--color-light); text-shadow: 2px 2px 8px rgba(0,0,0,0.8);">
          🔍 Alle rekvisitter
        </h1>
        <p class="text-base md:text-xl" style="color: var(--color-light); opacity: 0.9;">
          Udforsk rekvisitter fra teatre i hele Danmark
        </p>
      </div>
    </div>
  </section>

  <!-- Main content -->
  <main class="flex-grow container-max px-4 md:px-6 py-6 md:py-12">
    
    <section class="section max-w-6xl mx-auto">
      <!-- Filtrering -->
      <div class="bg-white rounded-2xl shadow-xl p-4 md:p-6 mb-6 md:mb-8">
        <!-- Basis filtre -->
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-3 md:gap-4 mb-4">
          <div>
            <label for="kategoriFilter" class="block text-sm mb-1" style="color: var(--color-dark); font-weight: 600;">Kategori</label>
            <select id="kategoriFilter" class="w-full px-3 md:px-4 py-2 rounded-xl border-2 text-sm md:text-base" style="border-color: var(--color-primary);">
              <option value="">Alle kategorier</option>
              <!-- Kategorier indlæses via JavaScript -->
            </select>
          </div>
          <div>
            <label for="lokationFilter" class="block text-sm mb-1" style="color: var(--color-dark); font-weight: 600;">Lokation</label>
            <select id="lokationFilter" class="w-full px-3 md:px-4 py-2 rounded-xl border-2 text-sm md:text-base" style="border-color: var(--color-primary);">
              <option value="">Alle områder</option>
              <option value="Hovedstadsområdet">Hovedstadsområdet</option>
              <option value="Nordsjælland">Nordsjælland</option>
              <option value="Sydsjælland og Lolland-Falster">Sydsjælland og Lolland-Falster</option>
              <option value="Vestsjælland">Vestsjælland</option>
            </select>
          </div>
          <div>
            <label for="soegning" class="block text-sm mb-1" style="color: var(--color-dark); font-weight: 600;">Søg</label>
            <input type="text" id="soegning" placeholder="Søg efter rekvisitter..." 
              class="w-full px-3 md:px-4 py-2 rounded-xl border-2 text-sm md:text-base" style="border-color: var(--color-primary);">
          </div>
          <div class="flex items-end">
            <button onclick="filtrerProdukter()" class="btn-primary w-full sm:w-auto py-2" style="border-radius: 100px;">
              🔍 Søg
            </button>
          </div>
        </div>
        
        <!-- Tilgængeligheds-filter -->
        <div class="pt-4 mt-4" style="border-top: 1px solid #e5e7eb;">
          <div class="flex items-center gap-2 mb-3">
            <span class="text-lg">📅</span>
            <h4 class="font-semibold" style="color: var(--color-dark);">Tjek tilgængelighed</h4>
            <label class="relative inline-flex items-center cursor-pointer ml-auto">
              <input type="checkbox" id="datoFilterAktiv" class="sr-only peer" onchange="toggleDatoFilter()">
              <div class="w-11 h-6 bg-gray-200 peer-focus:outline-none rounded-full peer peer-checked:after:translate-x-full peer-checked:after:border-white after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:border-gray-300 after:border after:rounded-full after:h-5 after:w-5 after:transition-all peer-checked:bg-green-500"></div>
            </label>
          </div>
          
          <div id="datoFilterContainer" class="hidden">
            <!-- Forestillingsperiode dropdown (kun for loggede brugere) -->
            <div id="forestillingsperiodeContainer" class="mb-3 hidden">
              <label for="forestillingsperiodeFilter" class="block text-sm mb-1" style="color: var(--color-dark); font-weight: 600;">Eller vælg din forestillingsperiode</label>
              <select id="forestillingsperiodeFilter" class="w-full px-3 md:px-4 py-2 rounded-xl border-2 text-sm md:text-base" style="border-color: var(--color-secondary);" onchange="valgForestillingsperiode()">
                <option value="">-- Vælg periode --</option>
                <!-- Perioder indlæses via JavaScript -->
              </select>
            </div>
            
            <!-- Manuel dato-valg -->
            <div class="grid grid-cols-1 sm:grid-cols-2 gap-3 max-w-full">
              <div class="min-w-0">
                <label for="datoFra" class="block text-xs md:text-sm mb-1" style="color: var(--color-dark); font-weight: 600;">Fra dato</label>
                <input type="date" id="datoFra" 
                  class="w-full px-2 md:px-4 py-1.5 md:py-2 rounded-xl border-2 text-xs md:text-base max-w-full" style="border-color: var(--color-secondary); box-sizing: border-box;">
              </div>
              <div class="min-w-0">
                <label for="datoTil" class="block text-xs md:text-sm mb-1" style="color: var(--color-dark); font-weight: 600;">Til dato</label>
                <input type="date" id="datoTil" 
                  class="w-full px-2 md:px-4 py-1.5 md:py-2 rounded-xl border-2 text-xs md:text-base max-w-full" style="border-color: var(--color-secondary); box-sizing: border-box;">
              </div>
            </div>
            
            <p class="text-xs mt-2" style="color: var(--color-dark); opacity: 0.6;">
              💡 Viser kun produkter der er ledige i den valgte periode
            </p>
          </div>
        </div>
      </div>
      
      <!-- Produkter grid -->
      <div id="produkterGrid" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 md:gap-6">
        <p style="color: var(--color-dark); opacity: 0.7;">Indlæser rekvisitter...</p>
      </div>
      
      <!-- Ingen produkter besked -->
      <div id="ingenProdukter" class="hidden text-center py-8 md:py-16">
        <div class="bg-white rounded-2xl shadow-xl p-6 md:p-10 max-w-lg mx-auto">
          <span class="text-4xl md:text-6xl mb-3 md:mb-4 block">🔍</span>
          <h3 class="text-xl md:text-2xl mb-3 md:mb-4" style="font-family: var(--font-heading); color: var(--color-primary);">Ingen rekvisitter fundet</h3>
          <p class="text-sm md:text-base" style="color: var(--color-dark); opacity: 0.8;">
            Prøv at ændre dine filtre eller søgeord.
          </p>
        </div>
      </div>
    </section>

  </main>

  <!-- Footer -->
  <%- include('../footer/footer') %>

  <script src="/compentens/js/kurv-global.js"></script>
  <script src="/compentens/js/rekvisitter.js"></script>
</body>
</html>
