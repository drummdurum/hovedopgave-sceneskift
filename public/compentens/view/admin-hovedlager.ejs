<!DOCTYPE html>
<html lang="da">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - SceneSkift</title>
  <link rel="stylesheet" href="/css/output.css">
</head>
<body class="flex flex-col min-h-screen" style="background-color: var(--color-light); font-family: var(--font-body);">
  
  <!-- Header -->
  <header class="sticky top-0 z-50" style="background-color: transparent; z-index: 50;">
    <%- include('../header/header') %>
  </header>
    
  <!-- Hero sektion -->
  <section class="relative min-h-[30vh] flex flex-col" style="background-image: url('/compentens/image/forside.webp'); background-size: cover; background-position: center; background-repeat: no-repeat; margin-top: -100px; margin-bottom: 40px;">
    <div class="absolute inset-0" style="background-color: rgba(0, 0, 0, 0.6); z-index: 0;"></div>
    <div class="flex flex-col items-center justify-center w-full px-6 flex-grow relative" style="z-index: 1;">
      <div class="text-center" style="padding: 80px 20px;">
        <h1 class="text-4xl md:text-5xl font-bold mb-4" style="font-family: var(--font-heading); color: var(--color-light); text-shadow: 2px 2px 8px rgba(0,0,0,0.8);">
          📦 Produkter på hovedlager
        </h1>
        <p class="text-xl" style="color: var(--color-light); opacity: 0.9;">
          Alle produkter opbevaret på Sceneskifts lager
        </p>
      </div>
    </div>
  </section>

  <!-- Main content -->
  <main class="flex-grow container-max px-6 py-12">
    
    <!-- Tilbage knap -->
    <div class="max-w-6xl mx-auto mb-8">
      <a href="/admin" class="inline-flex items-center gap-2 px-4 py-2 rounded-xl" style="background-color: var(--color-primary); color: white;">
        ← Tilbage til admin
      </a>
    </div>
    
    <!-- Produkter grid -->
    <section class="section max-w-6xl mx-auto">
      <div class="bg-white rounded-2xl shadow-xl p-10">
        <div class="flex items-center justify-between mb-6">
          <h2 class="text-3xl" style="font-family: var(--font-heading); color: var(--color-primary);">
            Produkter på lager
            <span id="produkterCount" class="text-xl font-normal" style="color: var(--color-dark); opacity: 0.7;"></span>
          </h2>
        </div>
        <div class="h-1 w-32 mb-8" style="background-color: var(--color-secondary);"></div>
        
        <div id="produkterGrid" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          <p style="color: var(--color-dark); opacity: 0.7;">Indlæser produkter...</p>
        </div>
        
        <div id="ingenProdukter" class="hidden text-center py-12">
          <p class="text-xl" style="color: var(--color-dark); opacity: 0.7;">Ingen produkter på hovedlageret endnu</p>
        </div>
      </div>
    </section>

  </main>

  <!-- Footer -->
  <%- include('../footer/footer') %>

  <script src="/compentens/js/admin-hovedlager.js"></script>
</body>
</html>
